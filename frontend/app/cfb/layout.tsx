export const metadata = {
  title: 'Texas Football Forecast - Travis Pollard',
  description:
    'An Elo model for college football, with every prediction written before kickoff and scored against the result. Predictions, accuracy, and the calibration curve.',
};

export default function CfbLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
